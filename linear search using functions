#linear search using functions
def linear(l,target):
    for ind in range(len(l)):
        if l[ind] == target:
            return ind
    return -1
l = [10,12,11,35,10,23,11]
target = 11
print(linear(l,target))
